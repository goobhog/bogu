name: Bogu CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install SBCL
        run: sudo apt-get update && sudo apt-get install -y sbcl

      - name: Run Bogu Master Diagnostics
        # We load the system, run the tests, and exit with a code 
        # based on the failure counter so GitHub knows if it passed.
        run: |
          sbcl --non-interactive \
               --load bogu/boot.lisp \
               --load bogu/tests.lisp \
               --eval "(bogu::run-bogu-tests)" \
               --eval "(uiop:quit (if (= bogu::*test-failures* 0) 0 1))"